# Trusted Solana mainnet RPC, used only on the server for current official holdings.
SOLANA_RPC_URL=
# Linked InsForge project URL and full-access admin API key. Both are server-only.
INSFORGE_URL=
INSFORGE_API_KEY=
# Random high-entropy bearer secret for aggregate research export.
RADAR_EXPORT_TOKEN=
# Optional legacy ActionKit route check. Radar never uses Jupiter.
JUPITER_API_KEY=
